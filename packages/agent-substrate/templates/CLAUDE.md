## Claude Code baseline

Provided by `@marcelle-labs/agent-substrate`. Shared Claude Code conventions for
Marcelle Labs repos. Repo-specific instructions go in this repo's own `CLAUDE.md`
sections, kept separate from this managed block.

- Read before you edit; match the surrounding code's conventions.
- Run the repo's lint/typecheck/test gates before declaring work done.
- Use feature branches; write commit messages as `type(scope): summary`.

This file is generated. Edit `templates/CLAUDE.md` in the substrate or override via
`.agents/CLAUDE.md`, then run `agent-substrate sync`.
