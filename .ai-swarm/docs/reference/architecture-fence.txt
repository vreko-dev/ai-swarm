# Architecture Fence
# This file is version-controlled and enforced by drift detection.
# Agents MUST NOT violate these boundaries.
# Customize this file for your project's package structure.

# === IMPORT BOUNDARIES ===
# List packages that MUST NOT import from other packages.
# Format:
#   apps/<app-name>/src/ MUST NOT import from:
#   @your-org/<package-name>  # reason: must go through service layer IPC

# Example: extension/VS Code app must not import intelligence directly
# apps/extension/src/ MUST NOT import from:
# @your-org/intelligence  # must route through daemon IPC
# @your-org/engine        # proprietary, internal only

# Example: MCP server must not import from extension
# apps/mcp-server/ MUST NOT import from:
# apps/extension/  # surface-agnostic constraint

# Example: core package must not import from surface-specific packages
# packages/core/ MUST NOT import from:
# vscode              # surface-agnostic constraint

# === CANONICAL LOCATIONS (single source of truth) ===
# List where specific values/definitions MUST live.
# Any re-definition elsewhere is a violation.

# Risk weights: packages/core/src/risk/weights.ts — no other file may define risk weight constants
# Schema registry: packages/contracts/src/ — all IPC/RPC method schemas
# Event types: packages/events/src/ — all domain event type definitions

# === REGISTRATION POINTS ===
# Any new RPC/IPC method MUST be registered in ALL of:
#   1. Service handler file
#   2. Schema/contracts file
#   3. Protocol union type

# Any new CLI command MUST be registered in ALL of:
#   1. Command handler file
#   2. CLI manifest/package.json

# === THIN CLIENT RULE ===
# If your project has a client-server architecture (e.g., VS Code extension + daemon):
# The client is a projection client. It renders server state, never computes it.
# All intelligence, scoring, and decision logic happens in the server.
# Client communicates ONLY via the official IPC/API layer.

# === PROJECT-SPECIFIC CONSTRAINTS ===
# Add your project's architectural invariants below.
# Use ENFORCE: prefix for constraints that can be grep-verified.

# ENFORCE: No direct database calls from API route handlers (must go through service layer)
# grep -rn "db\." apps/api/src/routes/ --include="*.ts" | grep -v "node_modules|dist" | wc -l == 0

# ENFORCE: All API endpoints use versioned prefix
# grep -rn '"/v1/' apps/api/src/routes/ --include="*.ts" | wc -l == 0  # no unversioned v1

# Add your project's specific enforcements here.
